export async function requestForRemarketing(url) {
  /**
   * Since it's an application for studying reactive
   * programming, this method is just an example
   * (so I left it without implementation)
   */
}